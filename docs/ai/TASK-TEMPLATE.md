# Task or handoff

Use only fields relevant to the work; reference files instead of copying them.
This note supplements the planned progress event store and never replaces it.

- Objective / task and atomic-step IDs:
- Owner / branch / claimed paths:
- For delegated work: selected model (Spark/Luna/Terra), routing reason, effort, tested revision/snapshot, isolated output paths:
- Required context and constraints:
- Acceptance criteria and required checks:
- Completed work / changed files:
- Verification commands, results, and evidence paths:
- Remaining work / blockers / risks:
- Next action / next eligible step:

For an authorized subagent, send its bounded objective, owned paths, applicable
constraints, and expected return evidence. Avoid a full transcript when a short
task packet suffices. Include additional context if correctness depends on it.
